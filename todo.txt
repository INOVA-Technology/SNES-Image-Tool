Image Creator:
	save color palette in local storage
	spell palette correctly everywhere
	clean up js/editor.js
	use `drawPixel' in `drawPixelWithEvent'
	fully update to ECMA6 (use let/const everywhere)
	fix bug when drawing on tiles on the edge of the canvas (the border of the canvas becomes to thick there)
	change the save/unsaved messages
	add @media stuff in css for smaller screens 
	add a way to change the pixel editor size and crap
	add a to-scall preview of the drawing
	add a way to change cursor size when drawing (requires a custom cursor)

